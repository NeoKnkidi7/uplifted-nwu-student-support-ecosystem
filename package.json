{
  "scripts": {
    // Remove TypeScript-specific scripts if any
    "build": "expo export --platform web"
  },
  "dependencies": {
    // Remove TypeScript and type definitions
  },
  "devDependencies": {
    // Remove TypeScript compiler and related packages
  }
}
